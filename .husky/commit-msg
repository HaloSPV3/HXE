[[ $(node --version) =~ ^v24 ]] || nvm use 2&> /dev/null ||
  [[ $(which nvm &> /dev/null ) ]] || 
  . /usr/share/nvm/init-nvm.sh &&
  [[ $(nvm use 2&> /dev/null) ]] || 
  nvm install && nvm use
yarn commitlint --edit $1